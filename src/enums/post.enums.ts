export enum PostVisibilityLevel {
  PUBLIC = "public",
  FRIEND = "friend",
  GROUP = "group",
}

export enum ReactionType {
  LIKE = "like",
  LOVE = "love",
  HAHA = "haha",
  ANGRY = "angry",
  UNREACT = "unreact",
}

export enum ReactionTargetType {
  POST = "post",
  COMMENT = "comment",
}
